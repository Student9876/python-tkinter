# It's not a good thing to import all the things in a library as it's done in the below 
from tkinter import *

# generally we type root = Tk()
zeon_root = Tk() # here we have created a base. Now we can add things in this base


# gui logic here

zeon_root.mainloop() #mainloop keeps you in the gui window and make it interactive

